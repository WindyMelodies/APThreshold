import numpy as np
import matplotlib.pyplot as plt

def phi_i_multiply_phi_k(x_array,option):
    """
    :param x_array:实验数据对应的自变量x，为数组类型
    :param option: 设置拟合函数为一次函数还是二次函数
    :return:计算结果为单个值
    """
    temp = 1.0
    for i in x_array:
        if option == "1":
            # 对应一次多项式的


def phi_i